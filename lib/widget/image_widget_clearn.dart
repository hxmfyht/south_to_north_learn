import 'package:flutter/material.dart';

class ImageWidgetClearn extends StatelessWidget {
  const ImageWidgetClearn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Container()
    );
  }
}
